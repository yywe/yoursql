read the logical_plan folder under expr, and do make_logical_plan.

refactor test code in session/mod.rs, prepare imple in-memory test database uinsg a  func, so later test, we do not need repeat code

at least 2 tables;


finalize expression and other utils

1.define the set of logical plans
2. impl plan_query
3. impl display for logical plan

